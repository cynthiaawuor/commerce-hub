# Copy to `.env` for local development.
# Both are relative: vite.config.ts proxies them to the services (see the proxy block).
VITE_PROCUREMENT_API_URL=/procurement-api
VITE_VENDOR_API_URL=/vendor-api

# Phase flag for this module. Anything other than "true" shows "Coming soon".
VITE_FEATURE_PROCUREMENT=true
